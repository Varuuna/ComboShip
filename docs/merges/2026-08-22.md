# 2026-08-22 — soh fix cherry-picks (ahead of the pin)

Not a merge pass. 18 upstream **fix** commits from the pending soh range `5a57a0cbc` → `55b52a26a`
(PR #144) were cherry-picked onto `develop` (branch `fix/soh-upstream-fix-picks`), skipping the
features/refactors and the Torch switch. Each commit carries `(cherry picked from Shipwright <sha>)`.

**The soh pin was NOT bumped** — it stays `5a57a0cbc`. The next full merge pass replays this range;
the picked changes are identical on both sides and auto-merge silently. Only the spots below, where
we deliberately deviated from the upstream file state, will surface again as conflicts — resolve
them the same way (or take upstream's side once the skipped feature lands with the full merge).

Picked: #7008, #7014, #6917, #6999, #7019, #7021, #7033, #7038, #7041, #7044, #7045, #7046, #7047,
#6932, #7052, #7053, #7054, #7049. Skipped fix-adjacent build-only commits: #7050/#7051 (warnings),
#7039 (clang `-lm`), #7024 (depends on the #7030 fmt switch we're not taking).

Hand-resolved conflicts (all were collateral from *skipped* feature commits sitting in patch context):

- `Enhancements/game-interactor/vanilla-behavior/GIVanillaBehavior.h` (#7038): dropped the
  `VB_NOT_CAST_FISHING`/`VB_FISHING_ZERO_XZ` enum entries (from skipped #7031 hover fishing).
- `Enhancements/randomizer/Messages/MerchantMessages.cpp` (#7044): re-seated our `COMBO_BUILD`
  `RG_COMBO_FOREIGN` block at the top of the final else, ahead of upstream's new `inShop` split, so
  shop slots still resolve foreign names (the first resolution attempt put it on the non-shop path
  only — caught in review).
- `Enhancements/randomizer/Traps.cpp` (#7044): took the `RG_TREASURE_GAME_*` trick names (enums
  exist in our tree); dropped the silver-rupee trick-name table (`RG_*_SILVER_*` from skipped #5659
  don't exist here → compile error).
- `Enhancements/AlwaysOnFixes.cpp` (#7052): took the `z_mir_ray.h` include; dropped the
  `UnregisterActorSkeletons` decl (context from skipped #6988).
- `Enhancements/debugger/debugSaveEditor.cpp` (#7053): applied the comment grammar fix only;
  dropped `DrawFlagArrayWithTooltips` (from a skipped commit, no callers here).
- `Enhancements/randomizer/Messages/ItemMessages.cpp` (#7053): took the French typo fix; kept our
  `int sceneNum;` (the `= -1` init belongs to a skipped commit).
- `location_access/dungeons/{dodongos_cavern,shadow_temple}.cpp` (#7053): kept ours — upstream's
  grammar fixes there live inside silver-rupee regions we don't have.
- `Enhancements/randomizer/option_descriptions.cpp` (#7053): dropped the `RSK_SHUFFLE_SILVER`
  description (enum absent); kept our Key Ring max 9/8 wording (the "plus one if Shuffle Chest
  Minigame" rewrite is #7020's rework); applied the dungon/Boss Keys typo fixes.

Combo-side follow-ups on the branch (build/review findings, not upstream changes):
`Traps.cpp GetTrickNamesEnglish` adapted to #7044's `TrickName{name, article}` element type, and the
`OTRGlobals.cpp` iceTrapModels-export comment retired (#7044 gives Chest Game keys trick names, so
they now export — Debug also loses a live `GetTrapName` assert there). Seeds are not reproducible
across this branch boundary (trick-name RNG draws changed); expected, no compat required.

The LUS side of PR #144 needed nothing: both its commits (incl. the #1212 wasapi hang fix) already
landed in the 2026-08-19 libultraship merge.
