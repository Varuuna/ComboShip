# Upstream merge — 2026-06-20

> Auto-generated by `upstream-merge.yml`. Fill in the per-folder post-merge changes
> (build-fix chain, COMBO_BUILD guards, runtime fixes) before merging, then link this
> file from docs/UPSTREAM_MERGES.md. See that doc for the policy.

- **libultraship**: up to date (`a3f1e102e`)

### soh — `adf31d5eb` → `74e1d4c20` (23 merges)

<details><summary>new commits</summary>

```
74e1d4c20 fix windows warning (#6781)
c3c80df37 Don't prevent child from accessing Ganon's Castle or GV Tent (#6691)
728a4f1c8 Complete rando starting items, make icon based like save editor (#6723)
c642e8c4d Remove all warnings on Windows and treat them as errors (#6733)
af6d46514 soh.o2r optimization part 1 (#6777)
e9bdda9c8 Woops on FPS items (#6779)
446c6c480 SkipIntro: still start at 12:00 in non-rando game (#6762)
65ffd5445 Ivan: Allow Ivan to break pots and crates while far from Link (#6768)
5a322418f mod menu group select (#6649)
00a51c650 fix imgui id conflicts (#6764) (#6771)
497d34570 fix: free look releases the door peek camera (#6756)
d912fd2c0 docs: document clang-format 14 setup and add an opt-in pre-commit hook (#6741)
4ea17f693 Dynamic Cosmetics Editor for Custom Models (#6429)
100bae3b9 feat(camera): option for free look to follow the default camera distance (#6757)
0744396e0 Redo Fishing Pole Gi Model (#6759)
6e6fee887 Fix Nabooru despawning from spirit when collecting an unrelated check (#6751)
ebdd2c34c fix(audio): replace stray custom-sequence printf with a debug log (#6752)
37ac3d51d Ivan: Megaton Hammer rework (#6753)
d7393015f Fix Growdirection (#6761)
bbb4c6499 fix id conflicts (#6765)
fa44542e6 Remove duplicate prefixes for rocks and trees (#6749)
948b84d8f asset purge (#6758)
e1462b07e fix and improve test builds on distros workflow (#6748)
```
</details>

**Conflict surface** (our customized files upstream touched):

```
CMakeLists.txt
soh/Enhancements/randomizer/SeedContext.cpp
soh/Enhancements/randomizer/draw.cpp
soh/Enhancements/randomizer/hook_handlers.cpp
soh/Network/Network.cpp
soh/OTRGlobals.cpp
soh/SaveManager.cpp
soh/SohGui/Menu.cpp
src/code/z_draw.c
```
- **mm**: up to date (`3545e62e0`)

## Post-merge changes

- **soh `Network.cpp`** — conflict resolved: kept the ComboShip `#ifdef…#else…#endif` block, adopted
  upstream's `static_cast<int>(strlen(payload) + 1)` inside the `#else`.
- **libultraship (build-fix chain)** — soh@develop now compiles with warnings-as-errors (`/WX`, soh
  #6733) and its `/WX` trips on two LUS headers we vendor. soh tracks LUS **`port-maintenance`** (which
  carries the fix) while we vendor LUS **`main`** (which doesn't yet), so we **back-ported Kenix3
  libultraship #1126 "Fix warnings"** into our vendored copy, additively:
  - `libultraship/include/fast/lus_gbi.h` — `#ifdef GIMMCMD / #undef` guard before the `GIMMCMD` define
    (fixes `C4005` macro redefinition).
  - `libultraship/include/ship/utils/binarytools/BitConverter.h` — `static_cast<int32_t>` on the two
    `ToUIntNNBE` offset args (fixes `C4267` conversion warning).
  - Each carries a `// ComboShip: back-port of Kenix3 #1126` comment; drop them once LUS `main` gains
    #1126. We deliberately did **not** switch to `port-maintenance` (it also carries #1103, which we skip).
- **Root `CMakeLists.txt` — build version is now DERIVED from the pins (save-format break fix).**
  Upstream #6723 ("Complete rando starting items") inserted 34 new `RANDO_ENUM_ITEM` keys (33
  `RSK_STARTING_*` after `RSK_STARTING_BEANS`, plus `RSK_STARTING_BUNNY_HOOD`) into the **middle** of
  `RandomizerSettingKey.h`, shifting the index of every key after the insertion point (incl.
  `RSK_TRIFORCE_HUNT`, `RSK_SHUFFLE_FISHING_POLE`) and growing `RSK_MAX` from 244 to 278. SoH serializes
  `randoSettings` as a flat array indexed by that enum, so any **pre-merge rando save** has a
  244-element array; post-merge `randoSettings[RSK_TRIFORCE_HUNT]` reads past the end → nlohmann returns
  `null` → `.get<uint8_t>()` throws → **crash on title-screen SRAM init** (`SaveManager.cpp:577`, via
  `Sram_InitSram` → `Save_Init` → `StartupCheckAndInitMeta`).
  - SoH's defense is the **stale-rando-save guard** (`StartupCheckAndInitMeta`, ~L508–529): it renames a
    rando save to `.bak` with a popup when its stored `buildVersion` differs from the running build.
    Upstream trips it via per-release version bumps; ComboShip had frozen `CMAKE_PROJECT_VERSION` at
    1.0.0, so it never fired.
  - **Fix:** the root `project(ComboShip VERSION …)` is now computed — `MAJOR=COMBO_EPOCH` (manual),
    `MINOR`/`PATCH` = first 4 hex of the soh/mm `mergedSha` in `upstream-pins.json`. This merge's soh pin
    (`74e1d4c20`) makes `MINOR=0x74E1=29921`, so the version is **`1.29921.13637`** — different from the
    pre-merge `1.0.0` saves, so the guard fires automatically. No vendored `SaveManager.cpp` edit, and
    **no per-merge bump going forward** (the pin moves every merge). See the UPSTREAM_MERGES.md standing
    policy "the build version is DERIVED from the pins". The opaque triple is labelled in-game via
    `PROJECT_BUILD_NAME` (`Combo Build e1 soh-74e1d4c mm-3545e62`) and in the package name
    (`ComboShip-e1-soh74e1d4c-mm3545e62`).
  - **Port-archive regen required by the version change.** `soh.o2r`/`2ship.o2r` are version-checked
    **full-triple** (`sohArchiveVersionMatch`/`shipArchiveVersionMatch`), so the new minor/patch made
    the existing `1.0.0` port archives read as "outdated" and `RunExtract` refused to launch. Fix:
    regenerated both via `GenerateSohOtr`/`Generate2ShipOtr` (re-extract with the new `--port-ver`).
    We deliberately kept the upstream full-triple port check (did **not** weaken it to MAJOR-only) —
    so regenerating the port archives is now a per-merge step. See UPSTREAM_MERGES.md.

- **soh `SohGui/Menu.cpp` — `Menu` ctor seeds `menuThemeIndex` under `COMBO_BUILD`.** SoH's new
  `THEME_COLOR` reads the cached `Menu::menuThemeIndex`, which is loaded from the `Menu.Theme` CVar only
  by `Menu::InitElement()`/`UpdateElement()`. Those run when the Gui registers/draws the menu — but our
  pre-existing `#ifndef COMBO_BUILD` guard in `SohGui::SetupMenu()` skips `gui->SetMenu(mSohMenu)`
  (comboui owns the menu), so `mSohMenu` is never registered and `InitElement()` never runs. That left
  `menuThemeIndex` uninitialized, and the **first** `THEME_COLOR` use — the new ImGui ROM-extraction
  modal drawn during `RunExtract` (`SohModalWindow::DrawElement` → `UIWidgets::PushStyleButton` →
  `ColorValues.at()`) — threw `std::out_of_range` and crashed `SOH_Init` on startup. Fix: seed
  `menuThemeIndex` from the `Menu.Theme` CVar in the `Menu` ctor (so `THEME_COLOR` is valid before any
  draw), `COMBO_BUILD`-guarded. Caveat: because `mSohMenu` is never drawn in ComboShip, this value is
  set once at construction; live theme changes flow through comboui, not SoH's `mSohMenu`.

- **mm `2s2h/Extractor/Extract.cpp` — `CallZapd` was passing `nullptr, nullptr` to `zapd_report`**
  instead of its `extractCount`/`totalExtract` params (a merge-resolution shortcut when upstream renamed
  `zapd_main` → `zapd_report` and added progress counters). That left MM's extraction progress counters
  permanently 0, so both MM's own `RunExtract` bar and ComboShip's MM extraction bar sat at "Starting
  Up" for the whole extraction. Fixed to pass the counters through, mirroring soh's `Extract.cpp`.

- **`combo/CMakeLists.txt` — clear `assets/` before the POST_BUILD copy (stale-extractor-xml trap).**
  The deploy uses `copy_directory`, which merges and NEVER deletes. When upstream renames/removes an
  extractor XML (here: mm `object_rs` → `object_rsn`), the old `object_rs.xml` lingers in the
  build-output `assets/xml/N64_US/objects/` forever; ZAPD then tries to extract it and fails with
  "Raw data size: 0x0 / reached end of file". (Same class first seen 2026-06-03, commit b333bec9b,
  which only added error-logging.) Fix: `rm -rf` the destination `assets/` before the copy_directory
  commands so the source tree is authoritative each build. The user's ROM/port archives live in the
  dir root (not under `assets/`), so clearing is safe.
